---
import Layout from './Layout.astro';

export interface Props {
  title: string;
  description?: string;
  image?: string;
}

const { title, description, image } = Astro.props;
---

<Layout title={`${title} | Blog SOPHIA`}>
  <div class="min-h-screen bg-gradient-to-b from-slate-100 to-slate-200 dark:from-slate-900 dark:to-slate-800">
    <!-- Blog Header -->
    <header class="relative py-16 bg-gradient-to-r from-slate-200 to-slate-100 dark:from-slate-800 dark:to-slate-900 border-b border-slate-300/50 dark:border-slate-700/50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center">
          <div class="flex items-center justify-center mb-4">
            <div class="w-12 h-12 bg-gradient-to-br from-amber-400 to-orange-500 rounded-full flex items-center justify-center mr-3">
              <span class="text-2xl">📚</span>
            </div>
            <h1 class="text-3xl md:text-4xl font-light text-slate-900 dark:text-white">Blog SOPHIA</h1>
          </div>
          <p class="text-xl text-amber-700/80 dark:text-amber-100/80 max-w-3xl mx-auto font-light">
            Reflexiones filosóficas, consejos de crecimiento personal y insights sobre el desarrollo de la consciencia
          </p>
        </div>
      </div>
    </header>

    <!-- Blog Content -->
    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
      <slot />
    </main>
  </div>
</Layout>

<style>
  /* Blog-specific styles */
  .blog-card {
    transition: all 0.3s ease;
  }

  .blog-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
  }

  .blog-content h2 {
    @apply text-2xl font-semibold text-slate-900 dark:text-white mb-4 mt-8;
  }

  .blog-content h3 {
    @apply text-xl font-semibold text-slate-900 dark:text-white mb-3 mt-6;
  }

  .blog-content p {
    @apply text-slate-700 dark:text-slate-300 leading-relaxed mb-4;
  }

  .blog-content ul, .blog-content ol {
    @apply text-slate-700 dark:text-slate-300 mb-4 pl-6;
  }

  .blog-content li {
    @apply mb-2;
  }

  .blog-content blockquote {
    @apply border-l-4 border-amber-400 pl-6 italic text-slate-700 dark:text-slate-300 my-6;
  }

  .blog-content code {
    @apply bg-slate-200 dark:bg-slate-700 px-2 py-1 rounded text-sm font-mono text-amber-800 dark:text-amber-300;
  }

  .blog-content pre {
    @apply bg-slate-100 dark:bg-slate-800 p-4 rounded-lg overflow-x-auto my-6;
  }

  .blog-content pre code {
    @apply bg-transparent px-0 py-0;
  }
</style>